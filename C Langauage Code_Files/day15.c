#include <stdio.h>

int main()
{
    
    FILE* ptr;

    // ptr=fopen("data.txt","r");
    ptr=fopen("data1.txt","a");

    // char str[50];
    // while (fgets(str, 100, ptr) != NULL) {
    //     printf("%s", str); // print file content
    // }
    
    // fclose(ptr);

    // fprintf(ptr,"Hello my name is Krish\n");
    // fprintf(ptr,"I am going to home\n");

    // FILE *fp = fopen("data.txt", "r");
    // char ch;
    // ch = fgetc(fp);
    // printf("Character: %c", ch);
    // fclose(fp);

    return 0;
}
