#include <stdio.h>

enum WeekDay{
        Sunday,
        Monday,
        Tuesday
};

int main(int argc, char const *argv[])
{
    // int a=8;
    // int arr[]={5,7,4};

    // typedef struct krish
    // {
    //     int age;
    //     float salary;
    //     char favChar;
    // } krish;

    // krish k1,k2;

    // k1.age=67;
    // k1.salary=22000;
    // k1.favChar='y';
    // printf("My age is: %d and salary is: %.1f and favChar is: %c\n",k1.age,k1.salary,k1.favChar);

    // k2.age=45;
    // k2.salary=28000;
    // k2.favChar='g';
    // printf("My age is: %d and salary is: %.1f and favChar is: %c\n",k2.age,k2.salary,k2.favChar);

    // union krish
    // {
    //     int age;
    //     float salary;
    //     char favChar;
    // };

    // union krish k1;

    // k1.age=67;
    // printf("My age is: %d\n",k1.age);
    // k1.salary=22000;
    // printf("My salary is: %f\n",k1.salary);
    // printf("My age is: %d\n",k1.age);
    // k1.favChar='y';
    // printf("My favchar is: %c\n",k1.age);

    // typedef unsigned int uint;

    // unsigned int a=567678;
    // uint a=567678;

    // typedef int* IntPtr;

    // IntPtr p1, p2; // both are int pointers
    // int a = 10, b = 20;
    // p1 = &a;
    // p2 = &b;
    // printf("%d %d", *p1, *p2);

    // enum WeekDay newday;
    // newday=Sunday;

    // printf("Day number: %d\n", newday);

    
    return 0;
}
